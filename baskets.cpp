#include <vector>
#include <cstddef>
#include <random>
#include "baskets.hpp"

namespace backtester {

std::vector<Basket> generate_baskets(std::size_t count, std::mt19937& gen) {
    std::vector<Basket> baskets;
    baskets.reserve(count);



    return baskets;
}

}
